import React from 'react';
import PropTypes from 'prop-types';

const Wishlist = props => {
    return (
        <div>
            <h2>Wishlist</h2>
        </div>
    );
};

Wishlist.propTypes = {
    
};

export default Wishlist;