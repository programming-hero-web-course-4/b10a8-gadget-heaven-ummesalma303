import React from 'react';
import PropTypes from 'prop-types';

const Cart = props => {
    return (
        <div>
            <h2>Cart</h2>
        </div>
    );
};

Cart.propTypes = {
    
};

export default Cart;